# Reviews

Keep the latest review and QA readiness artifact at:

`review.md`

Use frontmatter with `version`, `status`, `updated`, and `canonical: true`.
Archive milestone snapshots under `archive/` only when the previous version is
useful to preserve outside git history.
